{
  "name": "retail-fusion-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:frontend": "npm install --prefix frontend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  }
}
